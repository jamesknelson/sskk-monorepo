export function Page() {
  // TODO:
  // Show both address types, how they'll appear next to your message,
  // one with a number out the front, another with the and a photo.
  // Show the costs involved with the address listing, but do not ask for
  // billing information immediately.

  return <div>Select membership type</div>
}
