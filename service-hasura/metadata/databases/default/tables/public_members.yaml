table:
  name: members
  schema: public
object_relationships:
- name: profile
  using:
    foreign_key_constraint_on: profile_id
select_permissions:
- permission:
    columns:
    - created_at
    - id
    - profile_id
    filter:
      id:
        _eq: X-Hasura-User-Id
  role: member
