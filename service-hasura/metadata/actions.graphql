type Mutation {
  login(
    referrer_code: String
    referrer_source: String
  ): LoginOutput
}

input LoginInput {
  agent_token: String!
  languages: [String!]!
  referrer_code: String
  referrer_source: String
}

type LoginOutput {
  id: uuid!
  created_at: timestamptz!
  customer_id: uuid
}

